{
  "name": "mnemeory-mobradio",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling and scripts for the Mob Radio playlist generator.",
  "scripts": {
    "generate:mobradio": "node scripts/generate-mobradio.js",
    "build:ledger": "node scripts/build-ledger.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "js-yaml": "^4.1.1"
  }
}

