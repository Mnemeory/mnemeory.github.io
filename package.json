{
  "name": "mnemeory-mobradio",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling and scripts for the Mob Radio playlist generator.",
  "scripts": {
    "generate:mobradio": "node scripts/generate-mobradio.js",
    "validate:territory": "node scripts/validate-territory-statuses.js",
    "test": "npm run validate:territory"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
  }
}

