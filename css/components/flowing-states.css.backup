/**
 * Button Components
 * Canonical button components and variations for the Neural Interface
 * 
 * MIGRATION GUIDE:
 * - Previous class: .flow-action => Now use .neural-button
 * - Previous class: .flow-button-primary => Now use .neural-button.neural-button--primary
 * - Previous class: .neural-button-large => Now use .neural-button.neural-button--large
 */

/* === LIQUID SPACE CONTAINER (Legacy container - kept for backwards compatibility) === */
.liquid-space {
  position: relative;
  width: 100%;
  height: 100%;
  min-height: 400px;
  box-sizing: border-box;
  background: transparent; /* Made transparent to allow atmospheric effects to show through */
}

/* Only show liquid space background when starfield is active */
.starfield-view.active .liquid-space {
  background: transparent; /* Removed blue background to allow atmospheric effects to show through */
}

.consciousness-canvas {
  width: 100%;
  height: 100%;
  display: block;
  opacity: 1;
  z-index: var(--z-raised);
}

/* === NEURAL BUTTON SYSTEM === */

/* Base Neural Button - Foundation for all button variants */
.neural-button {
  /* Reset */
  background: none;
  border: none;
  margin: 0;

  /* Base Styling */
  padding: var(--space-3) var(--space-6);
  background: var(--glass-bg-light);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-organic-dynamic);
  color: var(--color-cyan);
  font-family: var(--font-ui);
  font-size: var(--text-sm);
  font-weight: 500;
  cursor: pointer;
  transition: all var(--duration-base) var(--ease-organic);
  position: relative;
  overflow: hidden;
  text-shadow: 0 0 5px var(--glow-cyan-gentle);

  /* Accessibility */
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
}

/* Neural Button Consciousness Flow Effect */
.neural-button::before {
  content: "";
  position: absolute;
  inset: 0;
  background: var(--gradient-consciousness-flow);
  opacity: 0;
  transition: opacity var(--duration-base) var(--ease-neural);
  z-index: -1;
}

/* Neural Button Hover States */
.neural-button:hover {
  background: var(--glass-bg-accent);
  border-color: var(--glow-cyan-border);
  color: var(--color-cyan);
  text-shadow: 0 0 8px var(--glow-cyan-gentle);
  transform: translateY(-1px);
  box-shadow: var(--shadow-neural-sm);
}

.neural-button:hover::before {
  opacity: 1;
}

.neural-button:active {
  transform: translateY(0);
  box-shadow: var(--shadow-neural-sm);
}

.neural-button:focus-visible {
  outline: 2px solid var(--color-accent);
  outline-offset: 2px;
  border-color: var(--color-accent);
}

/* Active/Pressed State */
.neural-button[aria-pressed="true"],
.neural-button.active {
  background: var(--glow-gold-error-bg-outer);
  border-color: var(--glow-gold-error-border);
  color: var(--color-warning);
  text-shadow: 0 0 5px var(--glow-gold-error-text);
}

/* === SIZE VARIANTS === */

.neural-button--small {
  padding: var(--space-1) var(--space-3);
  font-size: var(--text-xs);
}

.neural-button--large {
  padding: var(--space-4) var(--space-8);
  font-size: var(--text-lg);
  font-weight: 600;
  min-height: 60px;
}

.neural-button--large .btn-icon {
  font-size: var(--text-xl);
}

.neural-button--large:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-neural-lg);
}

/* Responsive adjustments for large buttons */
@media (max-width: 768px) {
  .neural-button--large {
    padding: var(--space-3) var(--space-6);
    font-size: var(--text-base);
    min-height: 50px;
  }
}

/* === COLOR VARIANTS === */

.neural-button--primary {
  background: var(--glass-bg-accent);
  border-color: var(--color-accent);
  color: var(--color-accent);
}

.neural-button--primary:hover {
  background: var(--glass-bg-medium);
  box-shadow: var(--shadow-neural-md);
}

.neural-button--success {
  background: var(--glass-bg-accent);
  border-color: var(--color-success);
  color: var(--color-success);
}

.neural-button--success:hover {
  background: var(--glass-bg-medium);
  text-shadow: 0 0 6px var(--color-success);
}

.neural-button--warning {
  background: var(--glow-gold-error-bg-outer);
  border-color: var(--color-error);
  color: var(--color-error);
}

.neural-button--warning:hover {
  background: var(--glow-gold-error-bg-inner);
  text-shadow: 0 0 6px var(--glow-gold-error-text);
}

.neural-button--gold {
  background: var(--glow-gold-error-bg-outer);
  border-color: var(--glow-gold-error-border);
  color: var(--color-warning);
  text-shadow: 0 0 5px var(--glow-gold-error-text);
}

.neural-button--gold:hover {
  box-shadow: var(--shadow-authority);
}

/* === CONTEXT VARIANTS === */

.neural-button--toolbar {
  padding: var(--space-2) var(--space-3);
  border-radius: var(--radius-organic-dynamic);
}

.neural-button--minimal {
  background: transparent;
  border-color: transparent;
  text-shadow: none;
}

.neural-button--minimal:hover {
  background: var(--glass-bg-light);
  border-color: var(--glass-border);
}

/* === LEGACY SUPPORT === */

/* Flow action button - Legacy support (deprecated) */
.flow-action {
  background: var(--gradient-bubble-base);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-organic-dynamic);
  cursor: pointer;
  padding: var(--space-4);
  color: var(--color-cyan);
  font-family: var(--font-ui);
  transition: all var(--duration-base) var(--ease-neural);
  min-width: 200px;
}

.flow-action:hover {
  background: var(--gradient-neural-primary);
  border-color: var(--glow-cyan-border);
  transform: scale(1.05);
  box-shadow: var(--shadow-neural-md);
}

.action-bubble {
  text-align: center;
  font-size: var(--text-sm);
  text-shadow: 0 0 4px var(--glow-cyan-gentle);
}
